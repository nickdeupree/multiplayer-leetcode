class Solution:

    # Time: O(?)
    # Space: O(?)
    def __init__(self, first_bad: int = 1) -> None:
        self.is_bad_version = lambda version: version >= first_bad

    # Time: O(?)
    # Space: O(?)
    def first_bad_version(self, n: int) -> int:
        # TODO: Implement first_bad_version
        return 1
